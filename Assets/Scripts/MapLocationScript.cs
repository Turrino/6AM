using UnityEngine;

public class MapLocationScript : MonoBehaviour
{
    //private Vector2 targetPosition;

    //void OnMouseDown()
    //{
    //    targetPosition = transform.position;
    //}

    //void Update() {
    //    if (targetPosition != null)
    //    {
    //        Master.GM.PlayerInstance.transform.position =
    //            Vector2.MoveTowards(Master.GM.PlayerInstance.transform.position, targetPosition, 10 * Time.deltaTime);
    //    }
    //        //Master.GM.PlayerInstance.transform.position = Vector3.MoveTowards(Master.GM.PlayerInstance.transform.position, targetPosition, 10 * Time.deltaTime);


    //}
}
